# The Apps Script web-app /exec URL, without any query string.
SHEET_API_URL=https://script.google.com/macros/s/XXXX/exec

# The token printed by showToken(). Server-side only — never exposed to the browser.
SHEET_API_TOKEN=xxxxxxxxxxxxxxxx
